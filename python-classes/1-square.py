#!/usr/bin/python3
"""This class does a bit more than it's forebearer, thus polishing the model that will give way to new objects"""


class Square:
    """This is a square, that will have a __size of __size"""

    def __init__(self, __size):
        self.__size = __size
