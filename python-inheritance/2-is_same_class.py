#!/usr/bin/python3
"""This function returns if a object is equal to a class"""


def is_same_class(obj, a_class):
    """This should do the trick"""
    if obj == a_class:
        return (obj == a_class)
    else:
        return (False)
